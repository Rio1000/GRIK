# Grik orchestrator (brain + voice + web). Runs on the host.
# The brain routes through Claude Code (via claude-agent-sdk) so it uses your
# Claude subscription, not paid API credits. The `anthropic` package stays for
# specialist Docker agents that still hit the API directly.
claude-agent-sdk>=0.2
anthropic>=0.40
httpx>=0.27
docker>=7.1
python-dotenv>=1.0

# web interface
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12

# voice stack (only needed for voice mode, not web mode)
openwakeword>=0.6         # wake word — open source, no API key needed
numpy>=1.24
sounddevice>=0.4          # mic capture + playback
faster-whisper>=1.0       # local speech-to-text
elevenlabs>=1.0           # Celtic-accented text-to-speech
