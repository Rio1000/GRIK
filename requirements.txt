# Grik orchestrator (brain + voice). Runs on the host so it can reach mic/speaker.
anthropic>=0.40
httpx>=0.27
docker>=7.1

# voice stack
pvporcupine>=3.0          # wake word ("Grik" / "Hey Grik")
pvrecorder>=1.2
sounddevice>=0.4          # mic capture + playback
faster-whisper>=1.0       # local speech-to-text
elevenlabs>=1.0           # Celtic-accented text-to-speech
